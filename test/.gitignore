# ── 環境變數（含 API Key，絕對不上傳）──
.env

# ── Python ──────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# ── 執行期狀態檔（runtime，每次啟動重建）──
active_llm_requests.json
active_scrapers.json
active_users.json

# ── 日誌與效能紀錄 ────────────────────────
search_logs.json
session_comparison_times.json
stage2_performance.json
user_peak.json
vector_blocking_test_results.json

# ── 爬蟲暫存資料 ─────────────────────────
momo.csv
pchome.csv

# ── 除錯截圖 ─────────────────────────────
*.png
*.jpg
*.jpeg

# ── 系統雜項 ─────────────────────────────
.DS_Store
Thumbs.db

# ── 部署設定（含本機路徑，不適合上傳）────
cloudflared-config.yml
